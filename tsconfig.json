{
  "compilerOptions": {
    "outDir": "./build",
    "allowJs": true,
    // es6 apparently needed to use `for (x of set)`.
    "target": "es6",
    // "noImplicitAny": true,
    // "noImplicitReturns": true,
    "allowJs": false,
    "noUnusedLocals": true,
    // I don't think the following is really necessary, but ts-jest keeps recommending it.
    "esModuleInterop": true
  },
  "include": ["js"], // path name
  "exclude": ["node_modules"]
}
